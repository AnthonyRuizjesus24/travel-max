<section id="buscador" class="search-section">
    <div class="container">
        <h2>Encuentra tu Destino Ideal</h2>
        <form class="search-form" id="search-form">
            <input type="text" id="search-input" placeholder="¿A dónde quieres ir?" aria-label="Buscar destino">
            <a href="/destinos" class="cta-button">Buscar</a>
        </form>
        <div id="search-results" class="search-results"></div>
    </div>
</section>

<style>
    .search-section {
        background: linear-gradient(135deg, #1a2a6c, #b21f1f, #fdbb2d);
    padding: 4rem 0;
    color: #fff;
    text-align: center;
    position: relative;
    z-index: 0;
}

.search-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.4);
    z-index: -1;
}

.search-section h2 {
    font-size: 2.5rem;
    margin-bottom: 2rem;
    font-weight: bold;
    text-transform: uppercase;
    color: #fff;
}

.search-form {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 1rem;
    max-width: 700px;
    margin: 0 auto;
}

.search-form input[type="text"] {
    flex-grow: 1;
    padding: 1rem 1.5rem;
    border: none;
    border-radius: 5px;
    font-size: 1rem;
    color: #333;
    background-color: #fff;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    transition: box-shadow 0.3s ease;
}

.search-form input[type="text"]::placeholder {
    color: #aaa;
}

.search-form input[type="text"]:focus {
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
    outline: none;
}

.cta-button {
    background-color: #fff;
    color: #333;
    padding: 1rem 2rem;
    border: none;
    border-radius: 5px;
    font-size: 1rem;
    font-weight: bold;
    text-transform: uppercase;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.3s ease;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
}

.cta-button:hover {
    background-color: #ff9a3c;
    transform: translateY(-2px);
}

.search-results {
    margin-top: 2rem;
    max-width: 700px;
    margin: 0 auto;
    color: #fff;
    text-align: left;
    background: rgba(255, 255, 255, 0.1);
    padding: 1rem;
    border-radius: 5px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
}

.search-results p {
    margin: 0;
    padding: 0.5rem 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.2);
}

.search-results p:last-child {
    border-bottom: none;
}
/* Estilos responsivos */
@media (max-width: 768px) 
{
    .search-section h2 {
        font-size: 2rem;
    }

    .search-form {
        max-width: 100%;
    }

    .search-form input[type="text"] {
        font-size: 0.9rem;
        padding: 0.8rem 1.2rem;
    }

    .cta-button {
        padding: 0.8rem 1.5rem;
        font-size: 0.9rem;
    }

    .search-results {
        max-width: 100%;
    }
}
@media (max-width: 480px) {
    .search-section h2 {
        font-size: 1.5rem;
    }

    .search-form input[type="text"] {
        font-size: 0.8rem;
        padding: 0.7rem 1rem;
    }

    .cta-button {
        padding: 0.6rem 1.2rem;
        font-size: 0.8rem;
    }
}
</style>